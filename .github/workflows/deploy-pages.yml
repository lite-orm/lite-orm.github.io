name: Deploy documentation site

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      kervix_ref:
        description: Kervix branch, tag, or commit to document
        required: true
        default: main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout site source
        uses: actions/checkout@v4
      - name: Checkout Kervix documentation source
        uses: actions/checkout@v4
        with:
          repository: kervix/kervix
          ref: ${{ inputs.kervix_ref || 'main' }}
          path: kervix-source
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Verify canonical example
        working-directory: kervix-source
        run: |
          mvn -pl kervix-processor -am -DskipTests install
          mvn -pl kervix-examples/basic-mapper -am -DskipTests compile
      - run: npm ci
      - name: Build static site
        env:
          KERVIX_SOURCE_DIR: ${{ github.workspace }}/kervix-source
        run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
